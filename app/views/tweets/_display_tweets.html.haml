:javascript
  //$(document).ready(function(){
    //console.log('ok we made it');
    //$('.tweet-form').on('submit', function(e){
     // console.log('submitted!');
    //});
  //});
  $(document).ready(function(){
    $('.tweet-form').on('submit', function(e) {
      e.preventDefault();
      var data = $(this).serialize();
      $.post('/replies/create', data, function(response) {
        //if response 
        // $(this).parents('.tweets').hide()
      }, 'json');
    });
  });
:css 
  .reply{
  display: none;
  }
-@tweets.each_with_index do |tweet, index|
  .columns.small-5.small-offset-3.center.tweets
    =tweet.html_block.html_safe
    =form_tag replies_create_path, id: "form-tweet-#{tweet.id}", class: 'tweet-form' do 
      .form-group
        Reply
        =text_area_tag :body, "@#{tweet.friend.username}", class: "full-width form-control resize-reply", id: "reply-box-#{index}"
      .form-group
        =hidden_field_tag :remote_tweet_id, tweet.remote_tweet_id
        =hidden_field_tag :user_id, tweet.user.id
        =hidden_field_tag :tweet_id, tweet.id
      .form-group
        =submit_tag "Reply", class: "yellow button full-width float-center reply", id: "tweet-reply-#{index}"

    %hr

.middle.center.clear-both=paginate @tweets
